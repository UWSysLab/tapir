syntax = "proto3";
option cc_enable_arenas = true;

package replication;

message Request {
     bytes op = 1;
     uint64 clientid = 2;
     uint64 clientreqid = 3;
}

message UnloggedRequest {
     bytes op = 1;
     uint64 clientid = 2;
     uint64 clientreqid = 3;
}
